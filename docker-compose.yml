version: '3.8'
services:
  webapp:
    build: .
    ports:
      - "8080:80"
    restart: always

  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    restart: always
